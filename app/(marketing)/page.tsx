/**
 * Marketing landing — placeholder for Phase 0.5. Real landing copy + hero
 * lands in Build Prompt 4.3.
 */
export default function MarketingLandingPage() {
  return (
    <section className="flex min-h-[60vh] flex-col items-start justify-center py-8 md:py-7">
      <p className="text-eyebrow">Marketing landing</p>
      <h1 className="text-display mt-2 max-w-2xl text-balance">
        The 90 days nobody trained you for.
      </h1>
      <p className="text-body-l text-mute mt-3 max-w-prose">
        Coming soon. Real landing copy ships in Phase 4.3 — for now this
        page exists so Phase 0 routes resolve and the marketing layout is
        verifiable.
      </p>
    </section>
  );
}
